const tableau1 = [1, 2, 3];
const tableau2 = ["a", "b", "c"];

// Fusion des deux tableaux en un seul tableau
const tableauFusionne = tableau1.concat(tableau2);

//le tableau fusionné dans la console
console.log(tableauFusionne);
